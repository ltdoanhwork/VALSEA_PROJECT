fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
python-multipart>=0.0.12
google-generativeai>=0.8.0
python-dotenv>=1.0.0

# Tests — included so one `pip install -r requirements.txt` satisfies pytest + respx + asyncio plugin
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.21.1
